/* @flow */
import styled from 'styled-components';

// override the Link component
const Link = styled.a`
  color: #fff;
  font-size: 23px;
  line-height: 25px;
  position: relative;
  top: 3px;
  margin: 0 5px;
`;

export default Link;
