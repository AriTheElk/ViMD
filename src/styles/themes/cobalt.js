import base from "./base";

/**
 * Cobalt Theme
 */
const cobalt = {
  ...base,
  header_background: "#00223E",
  editor_theme: "cobalt",
  preview_background: "HSL(207.1, 100%, 12.2%)",
};

export default cobalt;
